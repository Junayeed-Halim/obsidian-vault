It is helpful to understand what we have learned about the perception data model and its training algorithm in the generic framework of machine learning.

- 

Linear model hypothesis family 
 
 H
 
">: The linear assumption represents the straightforward idea to formulate the relationship between the data attributes and the target value. Given a data sample, the model summarises all information about the target in one number, which is the weighted sum of the data attributes.

Representation of individual linear hypotheses: In a perception model, a hypothesis is directly specified by the weights used in computing the weighted sum. Note how the weighted sum is linked to the prediction target in a perceptron is a fixed mechanism, i.e. making binary decisions by comparing to the threshold 0. This information belongs to the model family and does not need to be represented for individual perceptron hypothesis.

- 

Perceptron training algorithm -- searching in 
 
 H
 
">: in each update step, the parameters are adjusted, and this can be seen as moving from one point to another in a hypotheses space: 
 
 w
 
 o
 l
 d
 
 
 &#x2192;
 
 w
 
 n
 e
 w
 
 
"> .

- 

Criterion to measure the error -- the number of classification errors:

This error is used by the training algorithm. In each step, the "movement" of 
 w
"> is determined so that the prediction result of the perceptron model is rectified at one currently misclassified data sample (the change is along the direction of being rectified). Keep in mind that the update would lead to a new perceptron model and affect all predictions without guaranteed immediate individual consequences.
- The error rate of any hypothesis evaluated on the training dataset measures the in-sample errors. It is stochastically linked to the out-of-sample error. The uncertainty can be reduced by evaluating a larger training dataset but cannot be completely eliminated.

👉 The fourth element, data distribution, is independent of the choice of the learning models. So we do not discuss the data distribution in the context of the perceptron model.