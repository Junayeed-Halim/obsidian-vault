## Tutorials

The following tutorials show the realisation of the simple linear classification model.

## Build a perceptron

In this video, we convert the mathematical definition of a perceptron model into program implementation. 

## Train a perceptron

In this video, we implement the training iteration steps for a perceptron model.

## Wrap our perceptron in a data model object class

In this video, we wrap the perceptron learning functions into a proper machine learning model object with standard API's.